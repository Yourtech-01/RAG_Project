# LLM (free - Gemini)
google-generativeai>=0.7

# Embeddings (free - local, no API key) — CPU-only torch to keep image small
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.9.1+cpu
sentence-transformers>=3.0

# Vector store
chromadb>=0.5

# PDF parsing
pymupdf>=1.24

# API
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
